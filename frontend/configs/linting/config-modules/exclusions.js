/**
 * Exclusions Configuration
 *
 * POLICY: All exclusions must require an explanation comment.
 * Do not add exclusions without explaining why the rule cannot be followed.
 */

export default {
  // Allow html tags for email template editor as it renders sanitized HTML email previews
  'svelte/no-at-html-tags': 'off',
};
