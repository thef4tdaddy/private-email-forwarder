{
  "name": "receipt-forwarder-root",
  "version": "1.1.0",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build",
    "heroku-postbuild": "npm run build",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0",
    "prettier-plugin-svelte": "^3.4.0"
  }
}
